package ftns;

import java.time.LocalDate;

public class FullDayClient extends DayClient {
    public FullDayClient(String name, String surname, int bYear, LocalDate regDate, LocalDate expRegDate) {
        super(name, surname, bYear, regDate, expRegDate);
    }

//до 22:00, GYM, Pool, Group
}
